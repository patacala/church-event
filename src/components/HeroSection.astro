---
// Hero Section - Sección de bienvenida principal con diseño premium
---

<section class="hero min-h-screen flex flex-col items-center justify-center px-6 py-12 text-center relative overflow-hidden bg-gradient-navy particles-bg">
  <!-- Decoraciones ornamentales en esquinas -->
  <div class="absolute top-8 left-8 w-24 h-24 border-t-2 border-l-2 border-gold-light/30 rounded-tl-3xl opacity-60"></div>
  <div class="absolute top-8 right-8 w-24 h-24 border-t-2 border-r-2 border-gold-light/30 rounded-tr-3xl opacity-60"></div>
  <div class="absolute bottom-8 left-8 w-24 h-24 border-b-2 border-l-2 border-gold-light/30 rounded-bl-3xl opacity-60"></div>
  <div class="absolute bottom-8 right-8 w-24 h-24 border-b-2 border-r-2 border-gold-light/30 rounded-br-3xl opacity-60"></div>

  <!-- Efectos de brillo dorados -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-gold-main rounded-full filter blur-3xl animate-float"></div>
    <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-gold-light rounded-full filter blur-3xl animate-float" style="animation-delay: 3s;"></div>
  </div>

  <!-- Contenido principal -->
  <div class="relative z-10 max-w-5xl mx-auto space-y-10 animate-fadeInUp">
    <!-- Logo de la iglesia -->
    <div class="mb-6 animate-float">
      <img
        src="/icons/icon-512.png"
        alt="The Victory Church Logo"
        class="w-40 h-40 mx-auto mb-6 drop-shadow-2xl glow-gold-intense"
      />
    </div>

    <!-- Título principal con gradiente -->
    <div class="space-y-4">
      <h1 class="font-display text-4xl md:text-6xl lg:text-7xl font-black text-gold-gradient">
        Celebración del
      </h1>
      <h2 class="font-display text-6xl md:text-8xl lg:text-9xl font-black text-gold-gradient drop-shadow-2xl">
        7º Aniversario
      </h2>
    </div>

    <!-- Divisor ornamental -->
    <div class="flex items-center justify-center gap-4 my-8">
      <div class="h-px w-20 bg-gradient-to-r from-transparent to-gold-main"></div>
      <div class="w-3 h-3 rotate-45 bg-gold-main"></div>
      <div class="h-px w-20 bg-gradient-to-l from-transparent to-gold-main"></div>
    </div>

    <!-- Subtítulo -->
    <p class="font-heading text-xl md:text-2xl lg:text-3xl text-white/90 max-w-2xl mx-auto font-light tracking-wide">
      Estamos felices de tenerte en este evento especial
    </p>

    <!-- Fecha, hora y ubicación con diseño premium -->
    <div class="space-y-3 font-heading">
      <div class="text-2xl md:text-3xl text-gold-light font-bold tracking-wider">
        Sábado, 28 de Febrero - 6:30 PM
      </div>
      <div class="text-base md:text-lg text-white/70 font-medium italic">
        7380 NW 54th St, Miami, FL
      </div>
    </div>

    <!-- Botón CTA con gradiente y shimmer -->
    <div class="pt-10">
      <button
        id="scrollToForm"
        class="group relative bg-gradient-gold hover:opacity-90 text-navy-dark font-heading font-bold text-lg md:text-2xl px-16 py-6 rounded-full shadow-2xl transition-all duration-300 transform hover:scale-105 glow-gold-intense overflow-hidden"
      >
        <span class="relative z-10 flex items-center gap-3">
          Comenzar Registro
          <svg
            class="w-7 h-7 group-hover:translate-y-1 transition-transform"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
          </svg>
        </span>
        <div class="absolute inset-0 shimmer-effect"></div>
      </button>
    </div>

    <!-- Mensaje de bienvenida
    <p class="font-display text-3xl md:text-4xl text-gold-light italic mt-8">
      TE ESPERAMOS
    </p> -->
  </div>

  <!-- Indicador de scroll animado -->
  <div class="absolute bottom-12 left-1/2 transform -translate-x-1/2 animate-bounce z-10">
    <svg class="w-10 h-10 text-gold-main/80" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>

<script>
  // Scroll suave al formulario cuando se hace click en el botón
  const scrollBtn = document.getElementById('scrollToForm');
  if (scrollBtn) {
    scrollBtn.addEventListener('click', () => {
      const formSection = document.getElementById('registro-form');
      if (formSection) {
        formSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  }
</script>

<style>
  .hero {
    position: relative;
    background-attachment: fixed;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(212, 175, 55, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }
</style>
